name: .NET CI - Unit Tests

on:
  pull_request:
    branches: [ main, develop ]
  push:
    branches: [ main ]

permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore CleanArchitecture.Cmms.Microservices.sln

      - name: Build solution
        run: dotnet build CleanArchitecture.Cmms.Microservices.sln --configuration Release --no-restore

      - name: Run unit tests
        run: dotnet test CleanArchitecture.Cmms.Microservices.sln --configuration Release --no-build --filter "FullyQualifiedName~UnitTests" --collect:"XPlat Code Coverage" --logger "trx;LogFileName=unit_tests.trx" --verbosity normal

      - name: Upload Test Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: |
            **/TestResults/*
            **/coverage.cobertura.xml

      - name: Publish Test Results
        if: >
          github.event_name == 'push' ||
          (github.event_name == 'pull_request' &&
           github.event.pull_request.head.repo.full_name == github.repository)
        uses: dorny/test-reporter@v1
        with:
          name: Unit Tests
          path: '**/TestResults/*.trx'
          reporter: dotnet-trx
